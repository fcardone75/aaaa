These are the annotations defined on the deployment — and they are also present on the service.
I’ve just restarted the pod, so it should now have the annotations correctly applied at pod level as well.

Let me know if you still can’t see them.
